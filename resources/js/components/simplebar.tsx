import SimpleBar from 'simplebar-react';

// Custom-scrollbar wrapper. Re-exported so pages import it from one place (and
// the simplebar CSS stays a single import in `extra.css`).
export default SimpleBar;
